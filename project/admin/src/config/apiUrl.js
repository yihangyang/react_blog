let ipUrl = 'http://127.0.0.1:7001/admin/'

let servicePath = {
  checkLogin: ipUrl + 'checkLogin', // check login username and password
  getTypeInfo: ipUrl + 'getTypeInfo', // get article type info
}

export default servicePath